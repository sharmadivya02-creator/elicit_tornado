'use client';

import Scene from './3d/Scene';

/**
 * ELICIT '26 multi-project showcase.
 * A scroll-driven 3D tunnel: shader-based "portal" particle explosion,
 * glassy SDF portfolio cards (your event photos), GSAP hero text reveal,
 * and an HTML overlay with the ELICIT description.
 *
 * Kept as `CosmicTornado3D` so AboutView.tsx doesn't need any changes.
 */
export const CosmicTornado3D = () => {
  return <Scene />;
};